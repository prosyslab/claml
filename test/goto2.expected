// implicit typedef
// implicit typedef
// implicit typedef
// implicit typedef
// implicit typedef
#line 1 "goto2.c"
void f(int x, ) {
  if (x < 1)
    goto case_neg_1;
  else
    x++ x++;
case_neg_1 : {}
}
