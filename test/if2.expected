// implicit typedef
// implicit typedef
// implicit typedef
// implicit typedef
// implicit typedef
#line 1 "if2.c"
void f(int x, ) {
#line 2 "if2.c"
  int y;
_L:
  if (x < 1 && x > 10 || x == 0) {
    y = x + 1;
  } else {
    y = x + 2;
  }
}
